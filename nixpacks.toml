# Nixpacks auto-runs collectstatic/migrate saat build — env vars belum ada.
# migrate + collectstatic dipindah ke start.sh (jalan saat container start).
[variables]
DISABLE_COLLECTSTATIC = "1"

[phases.build]
cmds = []
